version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: insurance_qdrant
    ports:
      - "6333:6333" # Port HTTP
      - "6334:6334" # Port gRPC
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_data: